Create webpage
Look after Makefile
Summarize all options in comments
Documentation!
FindVectorsWithShortOrbit: schneller
Test StoredStabilizerChain
Test/Modify MakeGAPStabChain (maybe only use base and size?)
Disable or fix deterministic verification
Partition backtrack stuff?
Proper Random elements from StabChain

AddGeneratorsToStabilizerChain for trivial stabchains testen
Random element generation with pools etc. besser testen

spaeter:

Verification in Ordnung mit strongbelow
picklers?


Add benchmark tests like in matrixss.

Later:
 * deterministic verification
 * lots of methods using stabilizer chains
 * Soluble-Method
 * MeatAxe method

Test:


LoadPackage("genss");
LoadPackage("chop");

gens := AtlasGenerators("Fi22",6).generators;
g := Group(gens);
SetInfoLevel(InfoGenSS,3);
S := StabilizerChain(g);
s := AtlasStraightLineProgram("Fi22",1).program;
hgens := ResultOfStraightLineProgram(s,gens);
cgens := List(hgens,CMat);
m := Module(cgens);
r := Chop(m);
soc := SocleSeries(m,r.db);
v := Unpack(soc.basis[1]);
ConvertToVectorRep(v);
S := StabilizerChain(g,rec( Cand := rec( points := [v], ops := [OnRight])));
